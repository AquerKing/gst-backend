spring.application.name=GrassITBackend
server.port=8090
# Database connection info
spring.datasource.url=jdbc:mysql://debian:3306/grassit_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# If your MySQL container will be launched locally, uncomment the following line and comment the above one.
#spring.datasource.url=jdbc:mysql://localhost:3306/grassit_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=grassit_db_admin
# If you'd like to change the password of database(root user), also remember to modify docekr-compose.yml, keeping the configs the same.